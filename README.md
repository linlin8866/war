bash <(curl -L -s https://cdn.jsdelivr.net/gh/linlin8866/war@main/warp.sh)



warp status    # 查看 WARP 和 redsocks 状态
warp test      # 测试 Google 连通性
warp ip        # 查看直连 IP 和 WARP IP
warp restart   # 重启服务
warp uninstall # 彻底卸载 WARP 及所有配置
